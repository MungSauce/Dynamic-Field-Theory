# TRUECSS Original Composite V1

Status: EXPERIMENTAL / continuity-preserving composite probe

## Frozen subject

This probe intentionally returns to the original trueCSS physical subject instead of the later bit-substrate reinterpretation.

Frozen invariants:
- exactly 1,000,000 reusable physical nodes;
- one byte fixed width per node for this candidate, declared before source teaching;
- one active chronological page condition q;
- page length 1,000,000 source positions;
- no page bank or serialized page formation;
- no residual/correction stream;
- no source-sized position selector;
- no representational growth as pages advance;
- one target exposed to the learning law at a time;
- source-dependent terminal map, if needed, is serialized and counted;
- after freeze, replay uses only the frozen node substrate + counted metadata + generic decoder;
- exactness is required.

## Single improvement target

Only the settling/binding law is improved.

Imported compatible mechanism:
- ACT206 V2-style affine conditional relations;
- q, position k, and recursively regenerated prior-output context determine the relation;
- relation coefficients are generic and source-independent.

No APC/Riddler page-specific puzzle clues, no arithmetic residual stream, no EIS/MSF signal representation, and no 50-million-carrier bit-substrate reinterpretation are included.

## Frozen representation

Candidate node state:
- 1,000,000 nodes;
- 1 byte per node;
- node value in Z/206.

For each chronological target, the generic law generates two node addresses, two invertible coefficients, and a salt. Training imposes the relation required for the current terminal. If the fixed substrate cannot satisfy a new relation together with all prior relations, the candidate fails immediately as an interference/expressivity failure.

If the candidate survives the requested corpus span:
1. the training-only DSU constraint solver is collapsed into exactly one byte per physical node;
2. the frozen state is serialized;
3. a pre-freeze generic replay verifies the DSU is no longer required;
4. source is removed;
5. cold replay runs from the frozen state only.

## Acceptance

For the 100 MB probe:
- all 100,000,000 bytes must be absorbed without contradiction;
- frozen node count remains 1,000,000;
- node width remains 1 byte;
- no source-dependent page state exists outside the frozen object;
- cold replay emits exactly 100,000,000 bytes;
- recovered integrity hash equals the pre-removal source hash.

A contradiction is a valid measured falsification of this relation law, not permission to change the architecture.
