name: MSF recording v4 100MB cold replay

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'msf_recording_v4/**'
      - 'validation/msf_recording_v4_100mb.py'
      - '.github/workflows/msf-recording-v4-100mb.yml'

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Smoke test one-sample recording
        run: |
          python3 - <<'PY'
          from pathlib import Path
          Path('smoke.txt').write_bytes((b'alpha beta gamma delta\n' * 4096) + bytes(range(32,127))*50)
          PY
          PYTHONPATH=msf_recording_v4 python3 msf_recording_v4/orchestra_encoder.py smoke.txt smoke.msf -n 32
          cp smoke.txt smoke.expected
          rm smoke.txt
          PYTHONPATH=msf_recording_v4 python3 msf_recording_v4/audience_decoder.py smoke.msf smoke.recovered
          cmp smoke.expected smoke.recovered

      - name: Restore orchestra after smoke test
        run: git checkout -- msf_recording_v4/orchestra_encoder.py || true

      - name: Download canonical enwik9 and extract first 100MB
        run: |
          curl -L --fail --retry 4 --retry-delay 5 -o enwik9.zip https://mattmahoney.net/dc/enwik9.zip
          python3 - <<'PY'
          import hashlib,zipfile
          with zipfile.ZipFile('enwik9.zip') as z,z.open('enwik9') as src,open('enwik100m','wb') as out:
              left=100_000_000;h=hashlib.sha256()
              while left:
                  b=src.read(min(8<<20,left))
                  if not b:raise SystemExit('unexpected EOF')
                  out.write(b);h.update(b);left-=len(b)
          print('prefix_sha256='+h.hexdigest())
          PY
          test "$(stat -c%s enwik100m)" = "100000000"

      - name: Encode signal, erase orchestra/source, audience cold replay
        run: python3 validation/msf_recording_v4_100mb.py enwik100m

      - name: Publish measured report
        uses: actions/upload-artifact@v4
        with:
          name: msf-recording-v4-100mb-report
          path: msf_recording_v4_100mb_report.json
          if-no-files-found: error
